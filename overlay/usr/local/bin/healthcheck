#!/bin/sh

set -euo pipefail
export BF_E=`basename ${0}`


#======================================================================================================================
# Ping Postfix port to check it is healthy.
#======================================================================================================================

[[ "$(echo PING | nc localhost 587)" = "PONG" ]] && exit 0
exit 1
